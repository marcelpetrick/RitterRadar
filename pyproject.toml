[build-system]
requires = ["setuptools==84.0.0", "wheel==0.48.0"]
build-backend = "setuptools.build_meta"

[project]
name = "ritterradar"
version = "0.0.84"
description = "Discover German medieval markets, Renaissance festivals, and Viking fairs on an interactive map"
authors = [{name = "Marcel Petrick", email = "mail@marcelpetrick.it"}]
license = "GPL-3.0-or-later"
license-files = ["LICENSE"]
readme = "README.md"
requires-python = ">=3.12"
classifiers = [
    "Programming Language :: Python :: 3",
    "Programming Language :: Python :: 3.12",
    "Programming Language :: Python :: 3.13",
    "Programming Language :: Python :: 3.14",
    "Framework :: FastAPI",
]
dependencies = [
    "fastapi==0.141.1",
    "uvicorn[standard]==0.52.4",
    "sqlmodel==0.0.42",
    "sqlalchemy==2.0.52",
    "alembic==1.20.0",
    "pydantic-settings==2.15.0",
    "httpx==0.28.1",
    "beautifulsoup4==4.15.0",
    "lxml==6.1.3",
    "geopy==2.5.0",
    "pyyaml==6.0.3",
    "jinja2==3.1.6",
    "python-multipart==0.0.32",
    "aiofiles==25.1.0",
    "anyio==4.15.1",
    "icalendar==7.3.0",
]

[project.optional-dependencies]
dev = [
    "ruff==0.16.7",
    "mypy==2.3.1",
    "pytest==9.1.1",
    "pytest-asyncio==1.4.0",
    "pytest-cov==7.1.0",
    "httpx==0.28.1",
    "sphinx==9.1.0",
    "sphinx-rtd-theme==3.1.0",
    "types-beautifulsoup4==4.12.0.20250516",
    "types-PyYAML==6.0.12.20260906",
    "types-aiofiles==25.1.0.20260518",
]

[project.scripts]
ritterradar = "ritterradar.main:run"

[tool.setuptools.packages.find]
where = ["src"]

[tool.setuptools.package-data]
"ritterradar" = [
    "templates/**/*",
    "static/**/*",
]

[tool.ruff]
src = ["src"]
line-length = 100
target-version = "py312"

[tool.ruff.lint]
select = ["E", "W", "F", "I", "UP", "B", "C4", "SIM"]
ignore = ["B008"]

[tool.ruff.lint.isort]
known-first-party = ["ritterradar"]

[tool.mypy]
strict = true
python_version = "3.12"
files = ["src"]

[[tool.mypy.overrides]]
module = [
    "sqlmodel.*",
    "geopy.*",
    "alembic.*",
    "bs4.*",
    "aiofiles.*",
]
ignore_missing_imports = true

[tool.pytest.ini_options]
testpaths = ["tests"]
asyncio_mode = "auto"
addopts = [
    "--cov=ritterradar",
    "--cov-report=term-missing",
    "--cov-fail-under=90",
]

[tool.coverage.run]
source = ["src/ritterradar"]
omit = ["*/tests/*"]

[tool.coverage.report]
exclude_lines = [
    "pragma: no cover",
    "if TYPE_CHECKING:",
    "raise NotImplementedError",
]
